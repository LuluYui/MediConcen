import React, {memo} from 'react';
import {View, Text, ListView} from 'react-native';


const AddAppointment = ({ navigation }) => (
        <View>
            <Text> 
                This is the page for adding items 
            </Text>
        </View>
   
);
  
export default memo(AddAppointment);